import React from "react";
import VTemplate from "../reusable/VTemplate";
const Vsigns = ({ data }) => {
  return (
    <div>
      <VTemplate data={data}/>
    </div>
  );
};

export default Vsigns;
